pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "executing using sheel hello Jenkins"'
                sh 'whoiami'
            }
        }
    }
}